# Server Configuration
NODE_ENV=development
PORT=3000

# Database Configuration
DB_NAME=instapp-dev
DB_USER=instapp-developer
DB_PASS=PaSs111
DB_HOST=localhost
DB_PORT=5433

# JWT Secret (for authentication)
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=1d

# Upload limits
MAX_FILE_SIZE=5242880  # 5MB in bytes