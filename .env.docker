# Server Configuration
NODE_ENV=development
PORT=3000

# Database Configuration
DB_DATABASE=instapp-dev
DB_USERNAME=instapp-developer
DB_PASSWORD=PaSs111
DB_HOST=db
DB_PORT=5432
DB_DIALECT=postgres

# JWT Authentication
JWT_SECRET=change_this_to_a_secure_secret_in_production
JWT_EXPIRES_IN=1d
JWT_REFRESH_EXPIRES_IN=7d

# Logging
LOG_LEVEL=debug
LOG_FILE_PATH=storage/logs/combined.log
ERROR_LOG_FILE_PATH=storage/logs/error.log

# Upload Configuration
UPLOAD_PATH=storage/upload
MEDIA_PATH=storage/media
MAX_FILE_SIZE=5242880 # 5MB in bytes

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Security
BCRYPT_SALT_ROUNDS=10

# App URL
APP_URL=http://localhost:3000